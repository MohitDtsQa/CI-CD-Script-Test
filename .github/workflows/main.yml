# CI CD Script Test

name: CI CD Script Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
    
jobs:
   test-and-deploy:
       runs-on: ubuntu-latest
       timeout-minutes: 60

  script: |
    console.log('This is CI CD Script test');
